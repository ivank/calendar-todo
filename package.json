{
  "name": "calendar-todo",
  "packageManager": "yarn@4.0.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@yarnpkg/types": "^4.0.0"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "each": "yarn workspaces foreach --all --parallel --verbose run"
  }
}
